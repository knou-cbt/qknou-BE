# Chandra OCR — vLLM 서버 연동 전용 (--chandra-method vllm)
# vLLM 서버가 별도 실행 중인 환경에서 사용
chandra-ocr==0.2.0

# ──────────────────────────────────────────────────────────
# Local GPU HF 추론 (--chandra-method hf) 사용 시:
#   pip install "chandra-ocr[hf]==0.2.0"
# 이 파일의 chandra-ocr==0.2.0 과 동시에 설치하면 충돌하므로
# HF 환경에서는 이 파일 대신 위 명령을 단독으로 실행할 것
# ──────────────────────────────────────────────────────────
